# AGENTS.md

## Workflow

- After making changes to `ddl`, install the local binary with `cargo install --path crates/ddl` so local testing uses the latest build.
- When asked to raise a PR, use the GitHub CLI `gh` and target `main`.
